[project]
name = "intersection-simulation-backend"
version = "0.1.0"
description = "Backend skeleton for an intersection simulation project"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
]
